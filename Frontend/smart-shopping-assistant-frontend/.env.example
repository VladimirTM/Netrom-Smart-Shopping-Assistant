# Base URL for the backend API.
# MUST end with /api — all frontend paths start with / (e.g. /auth/login, /cart),
# so the full URL becomes: VITE_API_URL + path  →  http://localhost:5211/api/auth/login
VITE_API_URL=http://localhost:5211/api
